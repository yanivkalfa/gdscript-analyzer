# Rust
/target
**/target

# Node / napi
**/node_modules
*.node
**/pkg/
/dist
# Generated by `napi build --platform` (the platform-aware loader + types).
/bindings/node/index.js
/bindings/node/index.d.ts

# wasm-pack / wasm-bindgen output
**/pkg/

# mdBook
/docs/book

# OS / editor noise
.DS_Store
Thumbs.db
*.log

# Ad-hoc CI/release logs pasted at the repo root while debugging (never committed)
/Error
/ReleaseErrors
/ReleasePrErrors

# Committed (NOT ignored), listed here as a reminder:
#   Cargo.lock                      (workspace has binaries)
#   vendor/godot/**                 (the vendored engine API data)
#   crates/gdscript-api/src/generated.rs  (generated, but reviewed + committed)
#   .changeset/*.md                 (changeset intents)
