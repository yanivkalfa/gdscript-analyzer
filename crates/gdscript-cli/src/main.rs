//! `gdscript-cli` — the `gdscript` command (Phase 5): `check` / `lint` / `format` / `symbols`.
//!
//! A native client of `gdscript-ide` for CI and pre-commit. Being native, it owns `std::fs`
//! (reading files from disk) — the analyzer core never does.
//!
//! Phase 0: placeholder binary.

fn main() {
    eprintln!("gdscript: not implemented yet (arrives in Phase 5).");
}
